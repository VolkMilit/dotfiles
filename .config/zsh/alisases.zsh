#alias i='sudo aptitude -R install'
#alias p='sudo aptitude remove --purge'
#alias pp='sudo aptitude remove --purge `deborphan`'
#alias aps='aptitude search'
#alias update='sudo aptitude update && sudo apt-get upgrade'
alias e=exit
alias x=startx

## alias'ы для команд, не требующих коррекции, но требующих подтверждения
#alias mvi='nocorrect mv -i'      # переименование-перемещение c пogтвepжgeнueм
#alias cpi='nocorrect cp -iR'     # рекурсивное копирование с подтверждением
#alias rmi='nocorrect rm -i'      # удаление с подтверждением
#alias rmrf='nocorrect rm -fR'   # принудительное рекурсивное удаление
#alias mkdir='nocorrect mkdir'   # создание каталогов без коррекции
#alias copydir='nocorrect cp -r'

alias -s exe=wine
